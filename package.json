{
  "name": "neobanco",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
